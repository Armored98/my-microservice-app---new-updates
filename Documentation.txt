Permissions:
gcloud services enable container.googleapis.com
gcloud services enable artifactregistry.googleapis.com ?
gcloud services enable cloudbuild.googleapis.com ?

Upload Web-app files in gcloud CLI (See Attached or pasted?):
my-microservice-app

Create Docker Containers:
gcloud auth configure-docker

Frontend Container:
docker build -t gcr.io/kubernetesproject-470408/frontend:latest ./frontend
docker push gcr.io/kubernetesproject-470408/frontend:latest

Backend Container:
docker build -t gcr.io/kubernetesproject-470408/backend:latest ./backend
docker push gcr.io/kubernetesproject-470408/backend:latest

Create Kubernetes Cluster with containers:
gcloud container clusters create microservice-cluster --num-nodes=3 --zone=us-central1-a
gcloud container clusters get-credentials microservice-cluster --zone=us-central1-a

kubectl apply -f k8s/

Retrieve the External IP to access the website:
kubectl get service frontend-service

----

Automating Deployment Pipeline Steps (when commiting to main):
Create Github repo

Github Actions → Add workflow → Include the yml file

Create service account
Give permissions (Which ones??)
Add key (JSON)
Download key

Go to GitHub → your repo → Settings → Secrets and Variables → Actions → New repository secret
Name it exactly: GCP_CREDENTIALS
Paste the entire JSON from the file.
Save the secret.